# Fork mantenido de Coqui TTS (el paquete original "TTS" quedo sin mantenimiento).
# El import sigue siendo `from TTS.api import TTS`.
coqui-tts>=0.24.1
# coqui-tts 0.27.x aún usa APIs de transformers 4.x (isin_mps_friendly); transformers 5.x rompe.
transformers>=4.57,<5
fastapi>=0.110
uvicorn[standard]>=0.29
soundfile>=0.12
numpy>=1.24

# torch y torchaudio NO se fijan aqui a proposito: instalalos primero con el wheel que
# corresponda a tu CUDA (ver README), p. ej.:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
