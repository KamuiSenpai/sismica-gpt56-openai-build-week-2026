# Fork mantenido de Coqui TTS (el paquete original "TTS" quedo sin mantenimiento).
# El import sigue siendo `from TTS.api import TTS`.
coqui-tts>=0.24.1
fastapi>=0.110
uvicorn[standard]>=0.29
soundfile>=0.12
numpy>=1.24

# torch NO se fija aqui a proposito: instalalo primero con el wheel que corresponda
# a tu CUDA (ver README), p. ej.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
